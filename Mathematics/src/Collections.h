#pragma once
#include <map>
#include <vector>
#include <queue>

template<typename T>
using List = std::vector<T>;

template<typename T1, typename T2>
using Dictionary = std::map<T1, T2>;

template<typename T>
using Queue = std::queue<T>;